<p id="notice"><%= notice %></p>

<h1>Groups</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.id %></td>
        <td><%= group.title %></td>
        <td><%= group.description %></td>
        <td><%= link_to 'Show', group %></td>
       </tr>
    <% end %>
  </tbody>
</table>

<br>

